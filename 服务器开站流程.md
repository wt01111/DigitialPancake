# 电子煎饼服务器开站流程

这份流程面向第一次部署网站的站长。目标环境是 Ubuntu 24.04 LTS、4 GB 内存、6 Mbps 带宽、50 GB 磁盘，代码仓库为 <https://github.com/wt01111/DigitialPancake.git>。

当前代码已经完成 Windows 本地测试和 Ubuntu 24.04 GitHub CI 应用测试。Nginx、systemd、证书、备份与恢复尚未在真实 Ubuntu 服务器演练；完成本流程的全部验收前，不要宣称网站已经上线。

## 最少命令的推荐流程

当前预配置为公网 IP `43.142.159.194`、主域名 `digitalpancake.top`，以及腾讯企业邮箱发信账号 `pancakeking@digitalpancake.top`（`smtp.exmail.qq.com`、SSL 465）。仓库没有 SMTP 密码、最高管理员邮箱或密码。

ICP备案审核期间，先登录 Ubuntu 24.04 服务器运行以下命令：

```sh
sudo apt update && sudo apt install -y git
git clone https://github.com/wt01111/DigitialPancake.git
cd DigitialPancake
sudo bash ./deploy/quick-install.sh
```

脚本会询问最高管理员邮箱，并以隐藏输入方式询问 SMTP 密码和首次站长密码，安装 Node.js 24、构建应用、安装单实例 systemd 服务与备份定时器。它只让 Nginx 监听服务器的 `127.0.0.1:8080`，不会在备案期间通过公网 IP 开放完整站点。需要查看页面时，在自己的电脑建立 SSH 隧道：

```sh
ssh -L 8080:127.0.0.1:8080 <SSH用户>@43.142.159.194
```

再打开 <http://127.0.0.1:8080>。此时只能检查页面和健康状态，不能验证登录、注册等依赖生产 Secure Cookie 与真实 Origin 的流程；脚本不会为临时 HTTP 降低安全设置。

ICP备案通过、域名 A 记录已经指向 `43.142.159.194`，并且云安全组已放行 TCP 80/443 后，只需运行：

```sh
cd ~/DigitialPancake && git pull --ff-only && sudo bash ./deploy/enable-https.sh
```

该脚本会先核对域名 IPv4，之后才申请真实证书、启用 HTTPS、关闭本地预上线站点并重启应用。它不会覆盖已有环境文件、数据库、上传附件或站长。以下章节保留手工部署与排错细节；使用快速脚本时无需再逐项重复执行。

## 一、提前准备

准备以下信息，但不要把密码或密钥写进仓库、聊天或截图：

- 一台 Ubuntu 24.04 LTS 服务器及具有 `sudo` 权限的 SSH 账号；
- 一个准备作为网站唯一主地址的域名，例如 `example.com`；
- 是否使用 `www.example.com`。不用时不要创建该 DNS 记录，并删除 Nginx 模板中的 `www` 块；
- 云邮件或域名邮箱服务商提供的 SMTP 主机、端口、加密方式、用户名、应用专用密钥和发件地址；
- 最高管理员登录邮箱。它稍后填写到 `OWNER_EMAIL`，本文不会替你填写；
- GitHub 仓库中准备部署的完整提交 SHA。不要用一个会继续变化的分支名代替最终 SHA。

在云服务器防火墙或安全组放行 SSH、TCP 80 和 TCP 443。不要向公网开放 3001、SQLite 数据库或上传目录。服务器内的 UFW 等安装基础软件后再配置。

## 二、登录并安装基础软件

通过 SSH 登录服务器，先确认系统版本与资源：

```sh
cat /etc/os-release
uname -m
free -h
df -h /
```

`VERSION_ID` 应为 `24.04`，架构应为 `x86_64` 或 `aarch64`。然后安装基础软件：

```sh
sudo apt update
sudo apt upgrade
sudo apt install git nginx sqlite3 rsync curl unzip xz-utils ca-certificates certbot python3-certbot-nginx ufw
sudo systemctl status nginx --no-pager
```

启用 UFW 前先放行当前 SSH 连接，再直接放行 TCP 80/443，避免依赖尚未注册或名称不同的应用 profile：

```sh
sudo ufw allow OpenSSH
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
sudo ufw status
```

## 三、创建运行账号和持久化目录

应用使用不可登录的独立账号。代码归 root 所有，服务账号只能写数据库和私有上传目录：

```sh
id electronic-pancake >/dev/null 2>&1 || \
  sudo useradd --system --home-dir /var/lib/electronic-pancake \
    --shell /usr/sbin/nologin electronic-pancake

sudo install -d -o root -g root -m 0755 \
  /opt/electronic-pancake /opt/electronic-pancake/releases
sudo install -d -o electronic-pancake -g electronic-pancake -m 0700 \
  /var/lib/electronic-pancake /var/lib/electronic-pancake/uploads
sudo install -d -o root -g root -m 0700 \
  /etc/electronic-pancake /var/backups/electronic-pancake
```

## 四、下载代码并固定版本

先查看 GitHub 上要部署的提交 SHA，再在服务器执行。下面会把当前 `main` 下载到一个新的发布目录，然后固定为当时的完整 SHA：

```sh
release_id="$(date -u +%Y%m%dT%H%M%SZ)"
release_dir="/opt/electronic-pancake/releases/$release_id"
sudo git clone --branch main --single-branch \
  https://github.com/wt01111/DigitialPancake.git "$release_dir"
cd "$release_dir"
sudo git checkout --detach '<替换为已验收的完整提交SHA>'
sudo git rev-parse HEAD
```

最后一条输出必须与预期 SHA 完全相同。此时仓库已经存在，才能运行其中的部署脚本。

## 五、安装 Node.js 24 LTS 并构建

项目不使用 Ubuntu 默认的旧版 Node。脚本下载 Node.js 官方二进制，并用官方 `SHASUMS256.txt` 校验；默认版本为 24.21.0。Node.js 官方当前把 v24 标为 LTS。

```sh
cd "$release_dir"
sudo chmod 0755 deploy/*.sh
sudo ./deploy/install-node24.sh
/usr/local/bin/node --version
/usr/local/bin/npm --version
```

先安装完整锁定依赖用于构建，构建成功后删除开发依赖：

```sh
sudo chown -R root:root "$release_dir"
sudo /usr/local/bin/npm ci
sudo /usr/local/bin/npm run validate:content
sudo /usr/local/bin/npm run build
test -f dist/index.html
sudo /usr/local/bin/npm prune --omit=dev
```

不要在 `npm ci` 之后对整个发布目录执行 `find ... -type f -exec chmod 0644`；这会清除 `node_modules` 中命令文件的执行位。Git 保存源码执行位，npm 负责其依赖文件权限。应用进程仍以非 root 账号运行。

激活这个发布版本：

```sh
sudo ln -sfn "$release_dir" /opt/electronic-pancake/current.new
sudo mv -Tf /opt/electronic-pancake/current.new /opt/electronic-pancake/current
readlink -f /opt/electronic-pancake/current
```

## 六、配置域名 DNS

在 DNS 服务商控制台添加：

- 主域名的 `A` 记录指向服务器公网 IPv4；
- 服务器确实配置 IPv6 时才添加 `AAAA`；
- 需要 `www` 时，把它设置为指向主域名的 `CNAME`，否则不创建。

等待解析生效，再从自己的电脑和服务器检查：

```sh
getent ahosts example.com
```

后续所有 `example.com` 都必须替换为你的真实主域名。应用只接受一个 `PUBLIC_ORIGIN`；`www` 只做永久跳转，不能作为第二个应用地址。

## 七、配置生产环境文件

从示例复制到 Git 仓库之外：

```sh
sudo install -o root -g root -m 0600 \
  deploy/app.env.example /etc/electronic-pancake/app.env
sudo nano /etc/electronic-pancake/app.env
```

至少修改：

- `PUBLIC_ORIGIN=https://你的主域名`；
- `SESSION_SECRET`：在服务器运行 `openssl rand -hex 32`，只把输出写入环境文件；
- `OWNER_EMAIL`：填写最高管理员用于登录的邮箱；
- `OWNER_NICKNAME=Admin` 可保留，也可改显示昵称；
- `DATABASE_PATH` 与 `UPLOAD_DIR` 保持示例中的绝对路径。

不要在环境文件中设置 `OWNER_PASSWORD`。初始化时从终端隐藏输入，避免密码长期落盘。

## 八、配置云邮件和 DNS 邮件记录

在邮件服务商控制台创建并验证发信域名与发件地址。按服务商给出的精确值，在 DNS 中配置：

1. SPF TXT；同一域名不要发布多条互相冲突的 SPF 记录；
2. DKIM TXT 或 CNAME；名称和值必须与服务商一致；
3. `_dmarc` TXT。初次可采用服务商建议的监控策略，查看报告并确认正常后再逐步收紧；
4. 服务商要求的退信域名、MX 或验证记录。

等邮件服务商显示验证成功后，在 `/etc/electronic-pancake/app.env` 填写 `SMTP_HOST`、`SMTP_PORT`、`SMTP_SECURE`、`SMTP_USER`、`SMTP_PASS`、`SMTP_FROM`。465 端口通常使用 `SMTP_SECURE=true`；STARTTLS 端口通常使用 `false`，但必须以服务商说明为准。真实密码或应用专用密钥只保存在权限为 0600 的环境文件中。

SMTP 暂未配置时，网站可以先运行，但注册验证码与密码重置会返回 503。不要为此启用固定验证码或测试后门。

## 九、安装并启动应用服务

```sh
cd /opt/electronic-pancake/current
sudo install -o root -g root -m 0644 \
  deploy/electronic-pancake.service /etc/systemd/system/
sudo systemd-analyze verify /etc/systemd/system/electronic-pancake.service
sudo systemctl daemon-reload
sudo systemctl enable --now electronic-pancake.service
sudo systemctl status electronic-pancake.service --no-pager
curl --fail http://127.0.0.1:3001/healthz
```

只运行一个 Node 进程写 SQLite，不使用 PM2 cluster、多实例或共享网络数据库文件。失败时查看：

```sh
sudo journalctl -u electronic-pancake.service -n 100 --no-pager
```

## 十、安装 Nginx 配置

复制模板到系统配置目录，然后编辑所有 `example.com`：

```sh
sudo install -o root -g root -m 0644 \
  deploy/nginx/electronic-pancake.conf \
  /etc/nginx/sites-available/electronic-pancake.conf
sudo nano /etc/nginx/sites-available/electronic-pancake.conf
```

如果不用 `www`，删除文件开头整个 `server_name www.example.com` 的 server 块。如果使用 `www`，把主域名和别名都替换正确。模板的 CSP 允许正文与 Markdown 加载 HTTPS 图片，并允许同源 PDF.js worker，同时仍禁止第三方脚本、对象、框架和跨站表单；不要为了 PDF 预览或正文图片加入 `unsafe-eval`，也不要把 `script-src`、`worker-src`、`object-src` 改成宽泛的 `*`。PDF 和私有附件仍由登录鉴权 API 提供，不能把上传目录映射成 Nginx 静态目录。

启用配置：

```sh
sudo ln -sfn /etc/nginx/sites-available/electronic-pancake.conf \
  /etc/nginx/sites-enabled/electronic-pancake.conf
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
curl -I http://你的主域名/
```

## 十一、申请 HTTPS 证书

先确认公网能通过 80 端口访问域名。只使用主域名：

```sh
sudo certbot --nginx -d example.com
```

同时使用 `www`：

```sh
sudo certbot --nginx -d example.com -d www.example.com
```

然后验证自动续期：

```sh
sudo certbot renew --dry-run
curl -I https://example.com/
```

证书成功后，再确认 `/etc/electronic-pancake/app.env` 的 `PUBLIC_ORIGIN` 正好是同一个主域名，并重启应用：

```sh
sudo systemctl restart electronic-pancake.service
```

## 十二、初始化最高管理员

这一步只在首次建站，或站长明确重置最高管理员邮箱、昵称或密码时执行。系统使用 `OWNER_EMAIL` 登录，`Admin` 只是默认显示昵称。

```sh
sudo systemd-run --pty --wait --collect \
  --uid=electronic-pancake --gid=electronic-pancake \
  --working-directory=/opt/electronic-pancake/current \
  --property=EnvironmentFile=/etc/electronic-pancake/app.env \
  /usr/local/bin/node server/init-owner.js
```

在隐藏输入提示中输入密码。不要把密码放在命令行、文件、聊天或日志。下级管理员应先用已验证邮箱注册普通用户，再由最高管理员在“用户与管理员”页面升为管理员。管理员统一拥有文章、店铺、评价、评论、举报和赛题管理权；账号与管理员身份管理仍仅限最高管理员。

## 十三、安装备份计划

先人工执行一次备份并查看结果，再启用定时器：

```sh
cd /opt/electronic-pancake/current
sudo install -o root -g root -m 0644 \
  deploy/electronic-pancake-backup.service \
  deploy/electronic-pancake-backup.timer /etc/systemd/system/
sudo install -o root -g root -m 0644 \
  deploy/logrotate.conf /etc/logrotate.d/electronic-pancake
sudo /opt/electronic-pancake/current/deploy/backup.sh
sudo find /var/backups/electronic-pancake -maxdepth 2 -type f -print
sudo systemctl daemon-reload
sudo systemctl enable --now electronic-pancake-backup.timer
sudo systemctl list-timers electronic-pancake-backup.timer
```

备份包含 SQLite 一致快照与私有上传附件，默认保留 7 份，并保留至少 2 GiB 空闲空间。首次上线后应在隔离的 Ubuntu 24.04 测试机执行一次恢复演练，核对 SQLite `integrity_check`、附件哈希和 `/healthz`；不要第一次就在唯一生产数据上试恢复。

## 十四、逐项验收

- `https://主域名/` 能打开，HTTP 自动转 HTTPS；
- `www` 按选择永久跳转，或完全不存在；
- `curl http://127.0.0.1:3001/healthz` 成功，公网不能访问 3001；
- 用 `OWNER_EMAIL` 和刚设置的密码登录；
- 使用专用测试邮箱完成注册验证码、登录、密码重置；
- 新店铺、新评价、文章和赛题投稿进入审核，不直接公开；
- PDF 预览、正文 HTTPS 图片、鉴权附件下载正常；未登录不能下载私有文件；
- 最高管理员能把已验证普通用户升为管理员；管理员均可管理全部内容，但只有最高管理员能管理账号和管理员身份；
- Nginx 响应包含 CSP、`X-Content-Type-Options`、`X-Frame-Options` 等安全头；
- 手工备份成功，磁盘占用、Nginx 日志和 systemd 日志正常；
- 6 Mbps 下测试大附件体验，避免并发下载占满带宽。

## 十五、以后升级

升级不覆盖数据库、上传目录或环境文件。每次创建新发布目录，构建和验证后再切换符号链接：

```sh
release_id="$(date -u +%Y%m%dT%H%M%SZ)"
release_dir="/opt/electronic-pancake/releases/$release_id"
sudo git clone https://github.com/wt01111/DigitialPancake.git "$release_dir"
cd "$release_dir"
sudo git checkout --detach '<新的已验收完整提交SHA>'
sudo /usr/local/bin/npm ci
sudo /usr/local/bin/npm run validate:content
sudo /usr/local/bin/npm run build
sudo /usr/local/bin/npm prune --omit=dev
sudo chown -R root:root "$release_dir"
sudo chmod 0755 "$release_dir"/deploy/*.sh
sudo ln -sfn "$release_dir" /opt/electronic-pancake/current.new
sudo mv -Tf /opt/electronic-pancake/current.new /opt/electronic-pancake/current
sudo systemctl restart electronic-pancake.service
curl --fail http://127.0.0.1:3001/healthz
```

健康检查失败时，把 `/opt/electronic-pancake/current` 切回上一发布目录并重启服务。数据库变更前必须先备份；不要删除旧发布目录，直到新版稳定并完成恢复演练。

## 官方核对资料

- Node.js 发布状态：<https://nodejs.org/en/about/previous-releases>
- Ubuntu 24.04 Server 文档：<https://ubuntu.com/server/docs/>
- Ubuntu Nginx 安装文档：<https://ubuntu.com/server/docs/how-to/web-services/install-nginx/>
- Certbot Nginx 指引：<https://certbot.eff.org/instructions?ws=nginx&os=snap>
