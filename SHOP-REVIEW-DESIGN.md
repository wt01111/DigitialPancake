# 店铺口碑正式版规则

## 数据基线

原始表格整理为 76 家店铺、82 条历史意见。相同规范化链接或完全相同店名合并为一家店，原始意见仍逐条保留；相似但不同的店名不擅自合并。历史记录全部以 `approved` 导入，同时保留原表行号、类别、日期、原因、说明、备注和署名。

原表共 39 条红榜、41 条黑榜、2 条未分类。这些分类是历史意见，不代表本站核验，也不折算为本站用户星级。来源含“仅供参考，不得转发其他群或发布在其他平台”的提示；公开部署前仍需由站长确认内容使用授权。

## 公开查询

访客必须主动提交非空关键词，服务端才按店名、宣传名、掌柜 ID、渠道和经营范围返回匹配项。空关键词返回空结果；没有匹配项时不推荐其他店铺。

公开接口只返回已通过的店铺和评价。待审、退回、隐藏内容和购买凭证不可由公开接口读取。页面分别展示好评和差评数量及具体经历，也允许同一家店同时存在相反意见。历史意见始终显示“未经本站核实”，审核通过仅表示允许展示，不构成真伪判断或交易保证。

## 投稿、权限与附件

新店铺、新评价默认 `pending`，由有店铺审核权限的管理员审核后公开。评价人只能查看自己的提交和状态。审核日志记录操作者、对象、动作、时间与原因；隐藏或退回后立即从公开搜索和统计中排除。

购买凭证和附件存放在私有目录，经服务端校验会话、记录归属和权限后传输。Nginx 不映射上传目录。单文件上限 50 MiB，前置代理使用 52 MiB 请求体上限给 multipart 封装留空间，后端仍按文件本身执行限制。

站长 `Admin` 可创建、停用下级管理员并分配内容管理、店铺评价审核和举报处理权限。下级管理员不能管理管理员、授予权限或提升自己。前端路由和按钮只改善体验，所有读取、写入和权限判断均由服务端再次校验。

## 内容与邮件初始状态

真实文章和赛题初始为空，不用虚构内容填充。新文章和已发布文章的修订均需审核；修订通过前继续展示旧版本。固定演示验证码、演示账号、前端身份切换、本地存储权限和示例下载均从正式流程移除。

未配置 SMTP 时，注册、验证码、密码重置和安全邀请返回 503，不向页面或日志返回固定验证码。配置真实发信域名、账号和 TLS 后再启用，并对验证码获取、登录和注册限流。

## 运维边界

SQLite 使用 WAL 和单个 Node.js 进程，数据与上传保存在 `/var/lib/electronic-pancake`，不随版本目录替换。备份使用 SQLite 一致快照和不可变附件增量快照，并包含校验、有限保留和恢复演练；不能直接复制运行中的主数据库文件。完整操作见 `README.md` 和 `deploy/`。
