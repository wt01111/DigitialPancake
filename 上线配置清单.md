# 电子煎饼上线配置清单

这是一份交接核对表，不代表网站已经上线。当前部署材料按 Ubuntu 24.04 LTS、4 GB 内存、6 Mbps 带宽、50 GB 磁盘设计；尚未在真实 Linux 服务器完成 Nginx、systemd、证书、备份和恢复演练。

## 需要站长或服务器提供方确认

- [ ] 系统镜像确认为 **Ubuntu 24.04 LTS**，不是“24.02”。
- [ ] 服务器公网 IPv4：`________________`
- [ ] 可选公网 IPv6：`________________`
- [ ] 主域名：`________________`
- [ ] 是否同时使用 `www` 子域名：`是 / 否`
- [ ] DNS 管理平台与负责人：`________________`
- [ ] 80、443 端口可从公网访问；3001 不对公网开放。
- [ ] 磁盘告警接收人：`________________`（建议 80% 预警、90% 严重告警）

## 邮件服务的非密码信息

- [ ] SMTP 服务商：`________________`
- [ ] SMTP 主机名：`________________`
- [ ] SMTP 端口：`________________`
- [ ] 加密方式：`STARTTLS / TLS`
- [ ] 发件地址 `SMTP_FROM`：`________________`
- [ ] SMTP 用户名：`________________`
- [ ] 最高管理员登录邮箱 `OWNER_EMAIL`：`________________`
- [ ] 发信域名的 SPF、DKIM、DMARC 已按服务商要求配置。
- [ ] 已在域名邮箱或云邮件服务商后台核对发送额度、退信策略和发件人验证状态。
- [ ] 验证码邮件的回信或退信处理负责人：`________________`

SMTP 密码或应用专用密钥不要填写在本清单、聊天、截图、仓库或发布压缩包中。上线时由有权限的运维人员直接写入服务器 `/etc/electronic-pancake/app.env`，文件权限设为 `0600 root:root`。SMTP 尚未配置时，注册验证码和重置密码保持不可用并返回 503。下级管理员须先完成普通用户邮箱注册，再由最高管理员升为管理员；所有管理员统一管理内容，账号与管理员身份管理仅限最高管理员。

## 部署前

- [ ] 发布包来自最终验收后的白名单打包脚本，包内没有 `.env`、数据库、上传文件、日志、`node_modules` 或 `work`。
- [ ] Node.js 为受支持的 24 LTS，安装包校验通过。
- [ ] `PUBLIC_ORIGIN` 与最终 HTTPS 域名完全一致。
- [ ] `SESSION_SECRET` 在服务器生成，至少 32 字节随机值，未进入命令历史或发布包。
- [ ] `/var/lib/electronic-pancake` 属于服务账号且权限为 `0700`。
- [ ] 只运行一个 Node 进程写 SQLite；API 只监听 `127.0.0.1:3001`。
- [ ] Nginx 不映射私有上传目录，`/api/` 才反向代理到 Node。
- [ ] `npm run validate:content` 已验证 91 道官方赛题及所有本站附件的路径、大小和 SHA-256。
- [ ] 已确认 2025 年 H 题大图只指向官网原包，不计入本站磁盘和备份容量。

## 上线与验收

- [ ] 在 Ubuntu 24.04 上执行 `systemd-analyze verify` 和 `nginx -t`。
- [ ] DNS 生效后由 Certbot 获取真实证书，`certbot renew --dry-run` 通过。
- [ ] `/healthz`、首页、注册邮件、登录、密码重置、审核和鉴权下载通过。
- [ ] 使用 README 中的临时 systemd 命令，以服务账号初始化最高管理员；使用 `OWNER_EMAIL` 登录，`Admin` 仅为默认显示昵称，密码未写入任何文件或日志。
- [ ] 运行一次备份，核对 SHA-256 和 SQLite `integrity_check`。
- [ ] 在隔离环境完成一次恢复演练，并记录恢复用时、附件抽检与结果。
- [ ] 确认 6 Mbps 链路下 50 MiB 附件下载体验；单用户理论至少约 70 秒，并发时更慢。

完成上述 Linux 验证、真实域名证书和 SMTP 联调前，只能称为“已准备部署”，不能称为“已公网上线”。
